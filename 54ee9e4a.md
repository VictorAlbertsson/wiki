---
date: 2021-07-01T20:39
---

# Programming Language

```
%namespace("boat")

t_boat = enum (
    KETCH
    YAWL
    SLOOP
    SQRIG
    MOTOR
)

boat = struct (
    wll = int // water line length
    a = attrib // attribute
    sa = lng // sail area
)

motorboat(int(x),lng(y)) = function(boat(wll = x, a = boat_type.MOTOR, sa = y))

winners = array (
    boat(40, boat_type.YAWL, 6_000_000L)
    motorboat(28,0)
    boat(0)
)
```
